document.addEventListener( 'DOMContentLoaded', () => {

    const slider = new Slider({
        container: '.card-slider',
        slide: '.card',
        loop: true
        // pan: false,
        // click: false
    })

    console.log( slider )

} )